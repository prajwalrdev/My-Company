{
  "name": "contact-form-app",
  "version": "1.0.0",
  "description": "Contact form application with MongoDB integration",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mongodb": "^5.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 